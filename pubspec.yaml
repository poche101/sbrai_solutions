name: sbrai_solutions
description: "A new Flutter project for Store Hub Account Selection."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.5
  http: ^1.1.0
  intl: ^0.20.2
  file_picker: ^10.3.10
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Added the assets folder here
  assets:
    - assets/images/